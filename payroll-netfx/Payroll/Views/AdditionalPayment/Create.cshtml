@model Payroll.Models.d_AdditionalSalary

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>d_AdditionalSalary</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.AdditionalSalaryId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.AdditionalSalaryId)
            @Html.ValidationMessageFor(model => model.AdditionalSalaryId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.RateId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.RateId)
            @Html.ValidationMessageFor(model => model.RateId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.RateId, "m_Rate")
        </div>
        <div class="editor-field">
            @Html.DropDownList("RateId", String.Empty)
            @Html.ValidationMessageFor(model => model.RateId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.m_User.Username)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.m_User.Username)
            @Html.ValidationMessageFor(model => model.m_User.Username)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.m_User.Username)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.m_User.Username)
            @Html.ValidationMessageFor(model => model.m_User.Username)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
