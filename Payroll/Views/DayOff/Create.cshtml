@model Payroll.Models.d_DayOff

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>d_DayOff</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.DayOffId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DayOffId)
            @Html.ValidationMessageFor(model => model.DayOffId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Username, "m_User")
        </div>
        <div class="editor-field">
            @Html.DropDownList("Username", String.Empty)
            @Html.ValidationMessageFor(model => model.Username)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.DayOffDate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DayOffDate)
            @Html.ValidationMessageFor(model => model.DayOffDate)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
